"""
Legacy matching services package.

Do not import all legacy services here.
Only import modules explicitly where needed.

This avoids loading old architecture files that still depend on
legacy repositories, legacy DB access, or deprecated functions.
"""